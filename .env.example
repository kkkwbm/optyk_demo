# Environment Variables Template
# Copy this file based on your environment:
# - For local dev: Copy to .env.development (or just .env)
# - For production: Edit .env.production directly
# NEVER commit .env or .env.local to Git!

# =============================================================================
# API CONFIGURATION
# =============================================================================
# Backend API URL
# Local development: http://localhost:8080/api/v1
# Production: https://your-cloud-run-url/api/v1
VITE_API_BASE_URL=http://localhost:8080/api/v1

# =============================================================================
# GOOGLE MAPS API
# =============================================================================
# Google Maps API key (for location features)
# Get your API key from: https://console.cloud.google.com/google/maps-apis
# IMPORTANT: Restrict API key to your domain in production
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# =============================================================================
# DEVELOPMENT QUICK LOGIN CREDENTIALS (Optional)
# =============================================================================
# Quick login credentials for development mode
# IMPORTANT: Only used in development mode (import.meta.env.MODE === 'development')
# These credentials should be different from production passwords
#
# To enable quick login in development:
# 1. Copy this file to .env.development.local
# 2. Fill in your test account credentials below
# 3. .env.development.local is in .gitignore (won't be committed)
#
# VITE_DEV_ADMIN_EMAIL=admin@optyk.com
# VITE_DEV_ADMIN_PASSWORD=YourDevPassword123!
#
# VITE_DEV_EMPLOYEE_EMAIL=employee@optyk.com
# VITE_DEV_EMPLOYEE_PASSWORD=YourDevPassword123!
#
# VITE_DEV_MAINTENANCE_EMAIL=maintenance@optyk.com
# VITE_DEV_MAINTENANCE_PASSWORD=YourDevPassword123!

# =============================================================================
# NOTES
# =============================================================================
# 1. For local dev: Use .env.development (auto-loaded by Vite)
# 2. For production: Edit .env.production with production values
# 3. For local overrides: Use .env.development.local (already in .gitignore)
# 4. Vite only exposes variables prefixed with VITE_
# 5. See Vite docs: https://vitejs.dev/guide/env-and-mode.html
# 6. NEVER commit passwords or API keys to Git!
